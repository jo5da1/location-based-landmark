version: "3.9"

services:

  landmark-react-nearify:
    image: landmark-react-nearify
    container_name: landmark-react-nearify
    profiles:
      - landmark-react
    ports:
      - "3001:3000"
    environment:
      REACT_APP_LANDMARK_NEARBY_API_URL: 'http://localhost:8086/api/landmark'
      REACT_APP_ENDPOINT_NEARBY: '/nearby'
      REACT_APP_ENDPOINT_CATEGORY: '/category'
    networks:
      - landmark-net

networks:
  landmark-net:
    driver: bridge
  landmark-shared-net:
    external: true

# ####
# apk add --no-cache curl
# docker network connect landmark-shared-net landmark-react-nearify
# docker network connect landmark-shared-net landmark-nearby
# curl http://landmark-nearby:8086/api/landmark/category
# ping landmark-nearby
# curl http://host.docker.internal:8086/api/landmark/category
