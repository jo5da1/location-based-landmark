apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: landmark
spec:
  type: NodePort
  selector:
    app: pgadmin
  ports:
    - protocol: TCP
      port: 80          # internal port in cluster
      targetPort: 80    # container port
      nodePort: 30505   # external port on node

