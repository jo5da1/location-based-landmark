apiVersion: v1
kind: Service
metadata:
  name: landmark-geo-query-engine
  namespace: landmark
spec:
  type: NodePort
  #type: ClusterIP
  selector:
    app: landmark-geo-query-engine
  ports:
    - port: 8087
      nodePort: 30087
      targetPort: 8087
